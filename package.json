{
  "name": "dupla",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "start:dev": "pnpm --filter api run start:dev",
    "dev:web": "pnpm --filter web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:e2e": "pnpm -r run test:e2e",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:migrate": "pnpm --filter api exec prisma migrate dev",
    "db:status": "pnpm --filter api exec prisma migrate status",
    "db:verify": "pnpm --filter api exec prisma validate && pnpm --filter api exec prisma migrate status && pnpm --filter api exec prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code",
    "db:generate": "pnpm --filter api exec prisma generate",
    "db:studio": "pnpm --filter api exec prisma studio"
  },
  "devDependencies": {
    "prettier": "^3.9.5"
  }
}
